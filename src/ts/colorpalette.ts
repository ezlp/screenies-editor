/**
 * colorpalette.ts — MILESTONE 4 (not active yet)
 *
 * Manual palette from ssrp-editor: select text in a chatlog box, pick a
 * color, and a {RRGGBB} code is inserted around the selection. The M2
 * engine already renders those codes, so this is pure input convenience.
 */
export {};
