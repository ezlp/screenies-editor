/**
 * filters.ts — MILESTONE 3 (not active yet)
 *
 * Live sliders (0–200%): brightness, grayscale, sepia, saturate, contrast.
 * Preview uses canvas `ctx.filter` for instant feedback; the Rust export
 * pipeline (render/filters.rs) applies the same CSS-spec math so the saved
 * PNG matches the preview exactly.
 */
export {};
