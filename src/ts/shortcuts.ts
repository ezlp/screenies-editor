/**
 * shortcuts.ts — MILESTONE 4 (not active yet)
 *
 * Quick-text templates: create / apply / delete, stored as JSON via Rust
 * templates.rs (survives cache clears, shareable between users).
 */
export {};
