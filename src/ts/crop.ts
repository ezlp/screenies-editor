/**
 * crop.ts — MILESTONE 3 (not active yet)
 *
 * Crop box over the preview: presets 800×600 / 16:9 / 4:3 / 21:9 / 4K,
 * custom W×H (ratio-locked), and FREE mode (no lock) — per the agreed spec.
 * Last custom resolution is remembered via Rust config.rs.
 */
export {};
