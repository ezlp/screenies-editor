/**
 * backgrounds.ts — MILESTONE 3 (not active yet)
 *
 * Per-zone background modes from ssrp-editor:
 *   • block behind each line   • full-width mask   • text OUTSIDE the image
 *     (extra black canvas area so the screenshot stays 100% clean).
 */
export {};
