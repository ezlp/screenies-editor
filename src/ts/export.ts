/**
 * export.ts — MILESTONE 3 (not active yet)
 *
 * "Save Disk" and "Copy ke Clipboard". Gathers the full RenderJob (image,
 * crop, zones, styles, filters, stickers) and sends it to Rust
 * render/compose.rs for a pixel-perfect full-resolution PNG.
 */
export {};
