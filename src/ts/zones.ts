/**
 * zones.ts — reserved.
 *
 * The zone system (per-block position: Bebas + 4 sudut) shipped early and
 * lives in state.ts (Anchor / ChatBlock), chatlog.ts (per-block dropdown)
 * and canvas.ts (blockOrigin / drag). This file stays for future zone
 * extras — e.g. per-zone style overrides — planned around M4.
 */
export {};
