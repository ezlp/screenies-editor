# ScreeniesEditor 2.0 — Migration to a native Rust shell (egui)

**Goal:** replace the Tauri (WebView2/WebKitGTK) shell with a **native Rust**
shell so the app runs on old laptops with no webview — while keeping every
1.x feature and the Rust engine. The heavy lifting already lives in `core/`
(`screenies-core`): parser, render/export pipeline, fonts, clipboard, and the
new 2.0 additions. 2.0 swaps the *shell*, not the engine.

## Guiding rule: **2.0 is never a downgrade**

The migration is **strangler-fig**, not big-bang. The full-featured **Tauri
1.x app keeps shipping** (`src-tauri/` + `src/ts/**`) until the native shell
matches it feature-for-feature. Only then is Tauri retired. At no point does
the user lose a feature. (An earlier attempt removed Tauri up front — that was
reverted precisely because it made 2.0 a regression.)

## Architecture decision

**egui / eframe (native Rust) + `screenies-core`.**

| Option | Verdict |
|---|---|
| **egui / eframe** ✅ | Pure Rust, `cargo build` (no C++/Node). Small static binary; runs on old hardware. Already ran `core::compose::render` in the earlier native experiment. Immediate-mode UI is a fine fit for this tool. |
| Slint | Also pure Rust with a software renderer (great compat), declarative. Strong runner-up; picked egui for simplicity + prior proof. |
| Qt / CXX-Qt | Drags in a C++ toolchain + Qt libs (LGPL, heavier). Rejected: contradicts "lightweight + easy build". |
| Tauri (stay) | The webview is the whole problem we're migrating away from. Stays only as the interim shipping app. |

Everything visual is just a skin over `core`, so egui/Slint/Qt would all reuse
the same engine — nothing feature-wise depends on the choice.

## Screens (information architecture)

```
Main Menu (landing)   native/src/main.rs
├─ SSRP Editor        the current editor, ported onto core
├─ Chatlog Parser     open a chatlog FOLDER, index + search in-app (NEW)
└─ Gallery            browse exported SSRP photos by folder (NEW)
```

## Features already in `core` (shell-independent, unit-tested)

- **Effect filters** `blur` + `pixelate` — `render/filters.rs`. ✅
- **Text layout** — `render/layout.rs` (wrap + positioning, moved out of the
  old TS canvas; the real measurer `text.rs::GlyphMeasure` sums the same
  ab_glyph advances the renderer pens with → preview == PNG by identity). ✅
- **Chatlog library** — folder index + search, `chatlog_library.rs`. ✅
- **Gallery listing** — exported-photo folder scan, `gallery.rs`. ✅

Because these live in `core`, they can be wired into the **Tauri app now** (so
users get them before the native shell is done) *and* into the native shell.

## Phases

| Phase | Deliverable | Status |
|---|---|---|
| **0** | Native `egui` scaffold (landing menu → 3 stub screens); the `core` features above | ✅ |
| **1** | `screenies-native` builds + boots the menu on Linux; wire the new `core` features into the **Tauri app** so 1.x users get them now | |
| **2** | Native SSRP Editor: photo load, crop, text/chatlog, preview + export via `compose::render` (parity by identity) | ⏳ layout engine landed in `core` |
| **3** | Native Chatlog Parser: folder open, index, search + hover | |
| **4** | Native Gallery: folder browse, thumbnails, open-in-editor | |
| **5** | Settings/i18n/theming parity; effect-filter (blur/pixelate) sliders | |
| **6** | Windows + Linux packaging; **once at parity, remove `src-tauri/` + `src/ts/**`** and switch releases to native | |

## Native editor flow (how the preview stays identical to export)

The native shell holds editor state and, on any change:
1. builds `layout::LayoutBlock`s from the chatlog blocks;
2. `text::GlyphMeasure::new(font, size)` → `layout::layout_blocks(...)` →
   positioned `ExportBlock`s (+ `Bounds` for drag hit-testing);
3. assembles a `RenderJob` and calls `compose::render` — the SAME call the PNG
   export uses. The preview is that Rust-rendered image shown in egui, so
   preview and saved file are identical by construction (no separate UI-side
   layout to drift).

## Build / run

- Engine + Tauri app: `cargo test --workspace` (CI); `npm run tauri dev` (app).
- Native shell (excluded from the default workspace): `cargo run -p screenies-native`.
  Needs egui's Linux GUI deps (`libxkbcommon`, `libwayland`, GL, …); its own
  CI/packaging arrives in phase 6.

## Open questions

- egui immediate-mode look vs. a more "native" widget feel — acceptable for
  this tool; revisit theming in phase 5.
- Bundle strategy for the native binary (static vs. system libs) — phase 6.
