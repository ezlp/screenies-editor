//! templates.rs — MILESTONE 4 (not active yet)
//!
//! Quick-text shortcuts stored as templates.json in the app config dir —
//! shareable between users, survives cache clears (unlike localStorage).

#![allow(dead_code)]
